class PlayerPositionPackage
{
	string playerName;
	vector playerPosition;
	
	void PlayerPositionPackage(string playerName, vector playerPosition)
	{
		this.playerName = playerName;
		this.playerPosition = playerPosition;
	}
}